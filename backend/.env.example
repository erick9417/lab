# Configuración del Servidor Backend - Lucván LATAM
# Este archivo va en el servidor (NO en el repositorio)

# === Base de Datos MySQL ===
DB_HOST=localhost
DB_USER=lucvan5_admin
DB_PASSWORD=CAMBIAR_ESTO
DB_NAME=lucvan5_sistema

# === Directorio de Backups ===
# Ruta absoluta en el servidor
BACKUP_DIR=/home/lucvan5/backups

# === Email para recibir backups (SEPARATED ACCOUNT para disaster recovery) ===
BACKUP_EMAIL=backuplucvanlatam@gmail.com

# === Configuración SMTP (Gmail recomendado) ===
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=backuplucvanlatam@gmail.com
SMTP_PASSWORD=Y2ez5sJsL67n.
SMTP_FROM=Lucván Backups <backuplucvanlatam@gmail.com>

# === Servidor ===
PORT=4000
NODE_ENV=production

# === Seguridad JWT ===
JWT_SECRET=GENERAR_SECRETO_LARGO_ALEATORIO_AQUI

# === CORS ===
ALLOWED_ORIGINS=https://sistema.lucvanlatam.com,https://www.lucvanlatam.com

# Notas:
# 1. Crear carpeta de backups: mkdir -p /home/lucvan5/backups
# 2. Generar App Password de Gmail: https://myaccount.google.com/apppasswords
# 3. JWT_SECRET: usar `openssl rand -hex 32` para generar
# 4. Cambiar todas las contraseñas ANTES de usar en producción
